#include <algorithm>
#include <iostream>
#include <string>
#include <vector>

using namespace std;

template<typename T>
std::ostream & operator<<(std::ostream & s, const std::vector<T> & v) {
  //s << "[";
  for (size_t i = 0; i < v.size(); i++) {
    if (i > 0) {
      s << ", ";
    }
    s << v[i];
  }
  //s << "]";
  return s;
}

template<typename T>
vector<T> read_input() {
  return 0;
}
